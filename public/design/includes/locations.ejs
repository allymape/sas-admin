  <script>
    // $(function(){
        const regionCode = "<%= req.query.region || (location?.region || null)  %>"
        const districtCode = "<%= req.query.district || (location?.district || null)  %>"
        const wardCode = "<%= req.query.ward || (location?.ward || null) %>"
        const streetCode = "<%= req.query.street || (location?.street || null) %>"
        if(regionCode){
            getAllDistricts(regionCode , districtCode)
        }

        if(districtCode){
            getAllWards(districtCode , wardCode)
        }

        if(wardCode){
            getAllStreets(wardCode , streetCode)
        }
    // })
</script>
